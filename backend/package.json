{
  "name": "jcapas-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "bootstrap": "node scripts/bootstrap-d1.mjs",
    "migrate": "node scripts/migrate.mjs",
    "deploy": "wrangler deploy",
    "publish": "npm run bootstrap && npm run migrate && npm run deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
